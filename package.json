{
  "name": "lolance",
  "version": "1.0.0",
  "private": true,
  "description": "LOLance Premium Micro-Task Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx serve . -s -l tcp://0.0.0.0:3000",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npx playwright test",
    "lint": "find js/ -name '*.js' -exec node --check {} \\;"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "jsdom": "^26.1.0",
    "serve": "^14.2.6",
    "vitest": "^3.2.4"
  }
}
